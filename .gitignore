_site/
.jekyll-cache/
.sass-cache/
.DS_Store
vendor/
.bundle/

# Browser checks: installed by the job that runs them, not committed.
test/browser/node_modules/
test/browser/test-results/
test/browser/playwright-report/

# The schema.org clone that `rake schema:drift` reads. Cloned on demand by
# .github/workflows/schema-drift.yml; never committed.
/schemaorg/
