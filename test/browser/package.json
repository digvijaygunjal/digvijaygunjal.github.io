{
  "name": "recipes-browser-checks",
  "private": true,
  "description": "Browser checks for the built site. Installed only by the job that runs them; the rest of the suite is Ruby and needs no Node at all.",
  "scripts": {
    "test": "playwright test",
    "setup": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  }
}
